// Copy this file to TestSecrets.swift and fill in a valid API token for the
// local test server. TestSecrets.swift is gitignored.
enum TestSecrets {
    static let apiToken = "YOUR-API-TOKEN-HERE"
}

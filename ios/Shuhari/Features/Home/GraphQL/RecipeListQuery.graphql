query RecipeList(
  $type: RecipeType
  $category: DishCategory
  $sort: RecipeSort
  $order: SortOrder
  $limit: Int
  $after: RecipeId
) {
  recipes(
    type: $type
    category: $category
    sort: $sort
    order: $order
    limit: $limit
    after: $after
  ) {
    items {
      id
      title
      type
      category
      updatedAt
      versionCount
      bestNote
    }
    hasMore
    totalCount
  }
}

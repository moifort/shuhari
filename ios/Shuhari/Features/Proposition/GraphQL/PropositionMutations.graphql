mutation AcceptProposition($recipeId: RecipeId!, $proposition: PropositionInput!) {
  acceptProposition(recipeId: $recipeId, proposition: $proposition) {
    recipe {
      id
    }
    createdVersion
  }
}

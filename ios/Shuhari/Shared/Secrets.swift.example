// Copy to Secrets.swift (gitignored) and fill in if the app needs a static
// token for any admin-scoped call. Not required for the standard GraphQL API,
// which authenticates via the Firebase ID token.
enum Secrets {
    static let apiToken = "REPLACE-ME"
}
